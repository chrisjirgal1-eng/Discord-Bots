# Copy this file to .env and fill in your values

# Required — from Discord Developer Portal > Bot > Token
DISCORD_TOKEN=your_token_here

# Optional — paste your test server's ID here for instant slash command sync
# Leave empty to sync globally (takes ~1 hour to propagate)
DEV_GUILD_ID=
