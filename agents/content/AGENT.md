# Content agent (scaffold)

Status: defined, NOT running. Turn on by enabling its workflow when you want it spending time.

Owns: Zenthra + Clearcoat Co. content. When live, it pulls new ideas from the creator registry
(`memory-bank/creators.md`) + the video transcripts, then drafts posts. It does NOT invent new
logic -- it routes to the EXISTING skills: `content-pipeline` (scout -> topic -> hook -> script),
`caption`, `clearcoat-post`, `zen-announce`. Draft-only, never posts (the standing rule).

Escalates: nothing irreversible; outward-facing drafts always stop for Chris to send.

Reads/writes: the Content section of `agents/blackboard.md`.
